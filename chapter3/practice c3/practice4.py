# Replace the double space from problem 3 with single space

name="dhanish  is  good but a  bad boy"
print(name.replace('  ',' '))

print(name) #strings are immutable which means the og string can't be changed